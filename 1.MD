# MASTER FIX PROMPT — UNIVERSAL DYNAMIC DIRECTORY SCRAPER ENGINE

You are an expert in:

* Chrome Extension Development
* Dynamic Web Scraping
* React DOM Analysis
* Browser Automation
* DOM Mutation Systems
* Heuristic Data Extraction
* Lead Generation Systems
* Intelligent Crawling

CURRENT PROBLEM:
My extension fails on many business directory websites.

Example:

* IndiaMART
* Justdial
* Yelp
* Sulekha
* TradeIndia
* Google Maps

The scraper:

* cannot detect listings
* misses leads
* extracts empty data
* stops incorrectly
* fails on dynamic pages

IMPORTANT:
The issue is NOT only selectors.

The issue is:
MODERN DYNAMIC WEBSITE ARCHITECTURE.

---

## ROOT CAUSE ANALYSIS

Modern websites use:

* React
* Vue
* Angular
* AJAX rendering
* lazy loading
* virtual DOM
* delayed rendering
* infinite scrolling

Therefore:
document.querySelectorAll()
runs before listings fully load.

This causes:

* empty extraction
* wrong matches
* missing leads

---

## IMPORTANT FIX REQUIREMENT

DO NOT build:
basic selector scraper.

Build:
“INTELLIGENT DYNAMIC EXTRACTION ENGINE”

---

## NEW EXTRACTION ARCHITECTURE

The system must:

1. WAIT for dynamic content
2. OBSERVE DOM mutations
3. DETECT repeated business structures
4. ANALYZE rendered content
5. EXTRACT data after full rendering
6. HANDLE infinite scroll properly
7. VALIDATE extracted leads

---

## STEP 1 — SMART PAGE READY DETECTION

DO NOT scrape immediately.

Wait for:

* network idle
* DOM stabilization
* repeated business cards appearing

Create:
waitForListings()

The scraper should:

* monitor DOM changes
* detect listing growth
* begin extraction ONLY after listings stabilize

---

## STEP 2 — DOM MUTATION OBSERVER

Use:
MutationObserver

Observe:

* newly injected business cards
* AJAX-loaded content
* lazy-loaded listings

Continue observing UNTIL:

* listings stop increasing
* page stabilizes

---

## STEP 3 — HEURISTIC BUSINESS CARD DETECTION

DO NOT depend on:
.randomClass

Instead:
detect repeated structures automatically.

Find:

* repeated div layouts
* repeated article blocks
* repeated list items
* repeated card patterns

Use:

* DOM similarity scoring
* visual grouping heuristics
* text density analysis

---

## STEP 4 — SMART BUSINESS DETECTION

A valid business card usually contains:

* title text
* phone pattern
* address pattern
* website link
* business-related keywords

Calculate:
BUSINESS CONFIDENCE SCORE.

Only extract IF:
score > threshold.

---

## STEP 5 — ADVANCED DATA EXTRACTION

Extract:

* Business Name
* Phone Number
* Email
* Website
* Address
* Matched Keyword
* Relevance Score
* Source URL

---

## STEP 6 — DYNAMIC WAIT ENGINE

Before extraction:
wait for:

* React rendering
* lazy loading
* AJAX completion

Create:
smartRenderWait()

Use:

* requestAnimationFrame
* MutationObserver
* DOM stability timer

---

## STEP 8 — DUPLICATE PREVENTION

Track:

* URLs
* phone numbers
* business names

Avoid rescanning same listings.

---

## STEP 9 — RELEVANCE ENGINE

User enters:

* keyword
* optional city

The engine should:

* calculate keyword relevance
* validate product/service match
* reduce false leads

Use:

* Fuse.js
* fuzzy matching
* phrase matching
* synonym matching

---

## STEP 10 — FALSE LEAD FILTER

IGNORE:

* ads
* blogs
* random content
* navigation text
* unrelated cards

ONLY save:
high-confidence business leads.

---

## STEP 11 — SITE-ADAPTIVE EXTRACTION

Different websites use different structures.

Create:
ADAPTIVE SCRAPING ENGINE.

The engine must:

* automatically adapt to DOM structures
* detect repeating patterns dynamically
* avoid hardcoded selectors

---

## STEP 12 — FALLBACK EXTRACTION SYSTEM

IF primary extraction fails:

* try alternate card detection
* try text-density extraction
* try semantic extraction
* retry after DOM update

---

## STEP 13 — DEBUG SYSTEM

Add logs:

* listings detected
* DOM mutations count
* extraction success rate
* scroll attempts
* duplicate count
* stop reason

---

## STEP 14 — PERFORMANCE OPTIMIZATION

Avoid:

* full DOM rescans
* memory leaks
* endless MutationObservers
* unnecessary scrolling

Use:

* throttling
* batching
* incremental scanning

---

## IMPORTANT FILES TO FIX

Fix:

* universalScraper.js
* smartDetector.js
* infiniteScrollManager.js
* extractionFallback.js
* relevanceEngine.js
* content.js
* background.js

---

## IMPORTANT FINAL REQUIREMENT

The extension MUST:

* work on dynamic websites
* support React/Vue/Angular
* detect rendered business cards
* wait for AJAX content
* adapt to different directory websites
* reduce false leads
* stop scrolling intelligently
* generate accurate business leads

DO NOT generate basic scraper.

Generate:
REAL,
PRODUCTION-READY,
DYNAMIC EXTRACTION ENGINE.
